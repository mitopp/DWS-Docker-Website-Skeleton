# Redis configuration file
#
# Note that in order to read the configuration file, Redis must be
# started with the file path as first argument. (See `Dockerfile` CMD)
#
# link: https://github.com/redis/redis/blob/7.0/redis.conf
